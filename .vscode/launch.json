{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CC5X Build",
            "type": "shell",
            "request": "launch",
            "command": "${workspaceFolder}/scripts/cc5x-direct.sh",
            "args": [
                "${file}"
            ],
            "cwd": "${workspaceFolder}",
            "presentation": {
                "reveal": "always",
                "panel": "new",
                "clear": true
            }
        }
    ]
}